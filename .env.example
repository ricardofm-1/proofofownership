# Optional. Enables the WalletConnect option (QR code / mobile wallets).
# Create a free project at https://cloud.reown.com and paste the ID here.
# Without it, everything else — MetaMask, Phantom, and all verification —
# works exactly the same; WalletConnect is simply shown as unavailable.
VITE_WALLETCONNECT_PROJECT_ID=
